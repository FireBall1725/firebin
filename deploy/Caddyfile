# Caddy fetches and renews a Let's Encrypt certificate for $DOMAIN automatically.
# It needs ports 80 and 443 reachable from the internet and DNS pointing here.
{$DOMAIN} {
	reverse_proxy firebin-web:3000
}
